<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:weightSum="1"
    android:background="#838EC3"
    >

    <Button
        android:layout_width="400dp"
        android:layout_height="150dp"
        android:text="@string/loginButton"
        android:id="@+id/loginButton"
        android:background="@color/klubbColor"
        android:textColor="@color/vitText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:textSize="25sp"/>

    <Button
        android:layout_width="400dp"
        android:layout_height="150dp"
        android:text="@string/knappPub_text"
        android:id="@+id/knappPub"
        android:textColor="@color/vitText"
        android:background="#838EC3"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="300dp"
        android:textSize="25sp" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/adminLogin"
        android:id="@+id/adminButton"
        android:layout_alignParentBottom="true"
        android:background="@color/backgroundAnv" />

</RelativeLayout>